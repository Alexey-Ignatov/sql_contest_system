select birth_year, count(1)
from (
	select 
		customer_rk,
		max(year(birth_dt)) as birth_year,
		bit_or(monthly_income_amt=50000) as got50k, 
		bit_or(monthly_income_amt=60000) as got60k
	from cd_customers
	group by customer_rk
) as t1
where got50k and got60k
group by birth_year;