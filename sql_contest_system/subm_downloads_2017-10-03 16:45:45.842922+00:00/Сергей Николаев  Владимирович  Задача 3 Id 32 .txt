select 
	timestampdiff(year, birth_dt, current_date()) as age, 
	count(1) as cnt
from srcdt.cd_customers
where year(valid_to_dttm) = 5999
group by age
having age between 15 and 80
order by age desc;